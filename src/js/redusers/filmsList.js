
const initList = {
  startFetchList: false,
  fetchedList: false
}

 const filmsList = (state = initList, action) => {
  switch (action.type) {
  
    
    default:
      return state;
  } 
 }

 export default filmsList;